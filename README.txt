simple packaging for openvas-manager

The control file can be regenerated with:

   ./debian/rules debian/control DEB_AUTO_UPDATE_DEBIAN_CONTROL=yes

but it probably won't need to be.

https://svn.wald.intevation.org/svn/openvas/trunk/openvas-manager/packaging/debian/rules 
is for the v2 stuff. 
